// Module remplacé par src/lib/messaging.
// Utiliser `notify()` au lieu de `sendEmail()`.
//
// import { notify } from "@/lib/messaging";
// await notify({
//   channels: ["email"],
//   recipient: { email: "..." },
//   message: { subject: "...", body: "..." },
// });

export {};
